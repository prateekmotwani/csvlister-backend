CREATE TABLE name_links (  id int NOT NULL ,
                         name varchar(255),
                         link varchar(255),
                         PRIMARY KEY (id));

CREATE SEQUENCE hibernate_sequence;